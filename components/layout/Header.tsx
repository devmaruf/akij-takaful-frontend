import React from 'react';

type CardProps = {
    title: string,
    paragraph: string
  }

const Header = () => {
    return (
        <div>
            <h2>Header</h2>
        </div>
    );
};

export default Header;